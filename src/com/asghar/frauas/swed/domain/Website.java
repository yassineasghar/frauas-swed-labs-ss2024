package com.asghar.frauas.swed.domain;

/**
 * Represents a website by its URL.
 * a record ! why ? Website class cares about data, encaps. and that it
 * that's why record
 */
public record Website(String url) {
}
